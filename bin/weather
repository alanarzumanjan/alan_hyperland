#!/bin/sh

city="Riga"
url="wttr.in/${city}?format=3"
weather=$(curl -s --max-time 5 "$url")

if echo "$weather" | grep -qE "(Unknown|curl|HTML)"; then
    echo "WEATHER UNAVAILABLE"
else
    echo "$weather" | awk '{print $1" "$3}'
fi